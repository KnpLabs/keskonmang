version: '3.4'

services:
  nginx:
    build:
      context: ./
      dockerfile: .docker/Dockerfile
      target: nginx
      args:
        ENV_FILE: .env.prod.dist
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.nginx.rule=Host(`traefik.test`)"
      - "traefik.http.routers.nginx.entrypoints=web"
    networks:
      - traefik

  reverse-proxy:
    image: traefik:v2.0
    command:
      - "--api.insecure=true"
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--entrypoints.web.address=:80"
    ports:
      - "80:80"
      - "8080:8080"
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock"
    networks:
      - traefik

networks:
  traefik:
    external:
      name: traefik
